Following modules you need to download before executing the code :

@ pip install opencv-python
@ pip install scipy
@ pip install cmake
@ pip install imutils
@ pip install dlib  
@ pip install pygame (if you want to play alert sound)
@ pip install pyttsx3 (if you want to say the text according to you)